# SPEC: TRPGキャラシ作成サイト アップデート仕様

PLAN.md（音声メモ）を元に一問一答で確定した仕様。

## 機能要件

### FR1. 確認ダイアログの削減
- FR1.1 成功時のフィードバック（保存完了、各種コピー完了、共有リンク読み込み完了など）は全て非ブロッキングのトースト通知に置き換える。
- FR1.2 失敗時のフィードバックのうち、保存失敗・削除失敗はブロッキング `alert()` のまま維持する。
- FR1.3 それ以外の失敗（画像生成失敗、クリップボードコピー失敗、共有リンク読み込み失敗、ファイル読み込み失敗など）はトースト通知に置き換える。
- FR1.4 キャラクター削除時の `confirm()`、および新規作成による入力内容破棄時の `confirm()` はそのまま維持する（データ消失を伴う重要操作のため）。

### FR2. タブタイトルのキャラクター名同期
- FR2.1 名前欄の入力中、リアルタイムでブラウザタブのタイトルをキャラクター名に同期する。
- FR2.2 共有リンクからキャラクターデータを読み込んだ際にも、読み込んだ名前でタブタイトルを更新する。
- FR2.3 名前が未入力の場合は既存のデフォルトタイトル（例: 「シノビガミ キャラシ作成サイト」）を表示する。

## 非機能要件

- NFR1 既存方針を踏襲し、ビルドツール・バンドラ・フロントエンドフレームワークを導入しない（素のHTML/CSS/JSのまま）。
- NFR2 `magirogi` と `sinobigami` は共通モジュール化せず、既存の「並行実装（機能ごとに両ファイルへ同じロジックを重複実装）」パターンを踏襲する（トースト機構含む）。
- NFR3 新規外部ライブラリ依存を追加しない。

## スコープ外（今回は対応しない）

- OGP対応（共有リンクをDiscord等に貼った際のリンクプレビューにキャラ名・立ち絵を表示する機能）。将来目標として認識しているが、静的サイト構成では実現できず、Worker側の動的HTML生成という大きな変更が必要なため今回は見送る。
- アカウントページの新設、およびキャラクターのフォルダ／ディレクトリ分類機能。検討していたが取り下げ。バックエンド（`sinobigami-api`）側の変更は今回一切行わない。
