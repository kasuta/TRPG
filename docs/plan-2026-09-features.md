# 機能追加計画（2026-09-26〜）

音声入力で挙げた追加機能を、一問一答で詰めていく計画書。**決定事項は「決定事項」節に追記**し、未確定の点は「未確定の論点」節で管理する。
バックエンド（`kasuta/sinobigami-api`）の変更が必要になった分は、実装時に `sinobigami-api/docs/development-log.md` にも記録する。

---

## 要望の原文（要約）

### A. マイキャラクターの並べ替え
- 「あなたのキャラクター」欄は、現状は登録順（実際はサーバー側で `created_at DESC`＝新しい順）で並んでいる。
- これをドラッグで好きな順番に並べ替えたい。

### B. マイキャラクターのフォルダ
- 自分のPCと、GM用のボスなどを振り分けて置けるようにしたい。
- フォルダは自分で命名できる。階層は1階層まで。
- 「あなたのキャラクター」欄にフォルダとして表示され、クリックすると中身が見えて、そこから選択できるのが理想。
- 「フォルダ作成」ボタンを追加し、Aと同じドラッグ＆ドロップでキャラをフォルダに入れる。

### C. 判定ツール（`sinobigami_tool/`）の改装
- マイキャラクターと紐付ける。機能は総入れ替え。
- **C-1 判定値診断モード**: 現在の判定値（目標値＝5＋距離）の計算を使い、特技表の各マスを判定値で色分けする。5は薄く、6・7・8…と大きいほど濃くして、どこの判定が厳しいか一目で分かるようにする。
- **C-2 セッション用モード**: ボタンで「名前・キャラクター・指定特技」を登録し、そのボタンを押すと、何の特技で判定できるか（判定値）を一発で表示する。
  - シノビガミでは特技が列（分野）単位で使えなくなることがあるので、列ごとに「消える」ボタンを付け、押している間はその列の特技を使えないものとして自動計算する。
  - 登録したボタンは、キャラクター名が違えば別の色で表示する（どのキャラがその攻撃を打ったかを一目で判別するため）。

---

## 現状の確認（コード調査の結果）

- 一覧の取得: `GET /api/my-characters` は `ORDER BY created_at DESC`。並び順・フォルダを保存する列やテーブルは無い（`characters` は `id, data, image_url, created_at, user_id, updated_at, game`）。
- 一覧の描画: `sinobigami/index.js` の `renderListItems` / `renderMyCharacters` / `applyGameFilter`（マギロギ側も同じ構造の別コピー）。ゲーム種別のフィルタタブ（すべて/シノビガミ/マギロギ）がある。
- 判定ツール: `sinobigami_tool/index.html` の単一ファイル。現状はJSONファイルを読み込み、特技表のマスをクリックすると各キャラの習得特技ごとの目標値（5＋距離、ギャップの塗りつぶしを考慮）を表示する。バックエンドとの通信は無い。
  - 読み込むJSONは旧形式（`data.inputs` / `data.checkboxes`）。サーバーの保存形式（`compactifyForShare` の短縮形）とは異なる。
- ログイン情報: トークンは `localStorage` の `sinobigami_auth_token`（両アプリ共通）。ツールも同じオリジン（`kasuta/TRPG`）なので、同じトークンを読める。

---

## 決定事項

（一問一答で決まった内容をここに追記する）

| # | 論点 | 決定 | 補足 |
|---|---|---|---|
| Q1 | 並び順・フォルダの保存先 | **サーバー（D1）** | どの端末でも同じ並び・フォルダになる。API側のマイグレーションとデプロイが必要。既存のD1を使うため、新しい課金機能は使わない |
| Q2 | ゲーム種別との関係 | **両ゲーム共通の並び順・フォルダ1組。タブは絞り込みだけ** | フォルダにはシノビガミとマギロギのキャラを混ぜて入れられる。絞り込んでも、隠れたキャラの並びは変わらない |
| Q3 | 新しく保存したキャラの位置 | **一番上（フォルダの外）** | 今と同じく、新しいキャラほど上に来る |
| Q4 | スマホ（タッチ操作） | **PCだけ対応** | スマホでは並びの表示とフォルダの開閉だけ。並べ替えとフォルダへの移動はできなくてよい |
| Q5 | フォルダの表示方法 | **その場で開閉（字下げで展開）** | 複数のフォルダを同時に開ける。フォルダの外と中の間で、そのままドラッグできる。開閉の状態はブラウザに記憶する（仮。特に希望がなければこのまま） |
| Q6 | フォルダとキャラの並び | **自由に混ぜて並べる** | フォルダもキャラも同じ列で好きな位置に置ける。Q3の「一番上」は、フォルダも含めた一覧全体の一番上。ドロップ操作の案: フォルダ行の中央に落とすと「中へ入れる」、行の上端・下端に落とすと「その前後に置く」（挿入位置に線を表示） |
| Q7 | フォルダの名前変更・削除 | **名前はいつでも変更できる。削除は空のフォルダだけ** | 中にキャラがいると削除ボタンは押せない（先にキャラを外へ出す）。フォルダから出すのは、フォルダの外へドラッグする（Q5）。名前の重複は許可し、長さは30文字程度までとする（仮） |
| Q8 | 判定ツールの読み込み元 | **マイキャラ・共有リンク/ID・JSONファイルの3つ全部** | マイキャラはキャラシと同じログイン（`sinobigami_auth_token`）を使い、ツールでの再ログインは不要。共有リンクは他のプレイヤーのキャラ用。JSONはキャラシの「データを保存 (.json)」（`buildSaveData` の形式）で、今のツールと同じ。サーバーから読む分は短縮形なので、ツール側に `expandFromShare` 相当の変換が要る。CLAUDE.md の「ツールはバックエンドと通信しない」の記述は、実装時に更新する |
| Q9 | 判定値診断モードの内容と対象 | **理解どおり。新しいツールは1キャラだけ読み込む** | 各マスに「その特技が指定特技のときの最低目標値（5＋習得特技からの最短距離）」を表示し、値が大きいほど濃く塗る（習得特技のマスは5で最も薄い）。読み込めるのは1キャラのみ（別のキャラを読むと置き換え）。今の複数キャラ比較は無くなる |
| Q10 | 目標値の色分け | **5から順に濃くし、12以上はすべて最も濃い色** | 5〜12の8段階。マスには実際の数字（13など）も表示する（仮） |
| Q11 | セッション用モードの登録ボタン | **理解どおり。「キャラクター」は事前に名前を登録し、その一覧から選ぶ** | ボタン＝「名前（忍法名など）・キャラクター・指定特技（特技表から選ぶ）」。押すと、読み込んだ自分のキャラがその指定特技で判定するときの、使える特技と目標値を表示する。敵の攻撃の回避にも自分の忍法にも使える。キャラクター名を先に登録しておき、ボタン作成時はそこから選ぶ（表記ゆれを防ぐ）。ボタンの色はキャラクターごとに自動で分ける |
| Q12 | 登録内容の保存 | **ブラウザ（localStorage）に残す** | リロードしても残り、「すべてクリア」ボタンで消す。別の端末には引き継がない。登録は読み込んだキャラとは独立（キャラを替えても残る）、読み込んだキャラと列の状態もリロード後に復元する（仮） |
| Q13 | 列を消す状態の範囲 | **診断モードとセッション用モードで共通** | 列を消すボタンは両モードに表示し、状態も共通。消えた列の特技は判定の元に使えないだけで、距離はその列を通って数える |
| Q14 | マギロギへの対応 | **シノビガミだけ** | マイキャラ一覧からも、マギロギのキャラは選べないようにする。必要になったら後で追加する。今の機能（JSON複数読み込み・キャラカード・クリックで目標値）は、Q9のとおり新しい2モードに置き換える |
| Q15 | 実装の順番 | **A（並べ替え）→ B（フォルダ）→ C（判定ツール）** | AとBのAPI変更（並び順・フォルダの保存先）はまとめて設計し、マイグレーション1回で済ませる。Cのマイキャラ選択ではフォルダも使う |

---

## 未確定の論点

主な論点は Q1〜Q15 で決まった。表の「（仮）」の項目は、特に希望がなければそのまま進める。

- 開閉状態をブラウザに記憶する（Q5）
- フォルダ名の重複を許可し、長さは30文字程度まで（Q7）
- 目標値13以上のマスにも実際の数字を表示する（Q10）
- 読み込んだキャラと列の状態も、リロード後に復元する（Q12）

---

## 次の作業

1. A（並べ替え）: 下の「機能A 実装計画」のとおり進める。A・B共通のAPIもここで作る。
2. B（フォルダ）: フロントの変更だけで作る（APIの形はAで定義済み）。シノビガミとマギロギの `index.js` の両方に入れる。
3. C（判定ツール）: `sinobigami_tool/index.html` を作り直す。

---

## 機能A 実装計画（2026-09-26 承認）

### 設計
- **並び順はユーザーごとに1件のJSON（レイアウト）として保存する**。新テーブル `character_layouts(user_id PK, layout TEXT, updated_at)` を追加する。
  - フォルダ（B）も同じJSONに入る形で、今回まとめて定義する。Bではマイグレーションもデプロイも不要になる（Q15）。
  - レイアウトに無いキャラは「一番上・新しい順」に置く。そのため、新規キャラが一番上に来る仕様（Q3）は、保存処理を変えなくても成り立つ。削除済みのIDは、読み出し時に捨てる。
- **並び順の組み立て（マージ）はサーバーで行う**。フロントには自動テストが無いので、ロジックはテストのあるAPI側に置く。
- 既存の `GET /api/my-characters` は変更しない（キャッシュされた古いフロントも動く）。
- API:
  - `GET /api/my-layout`（要ログイン）: 並び順を適用したツリー `{ v: 1, items: [ {type:"character", id, name, game, updatedAt, createdAt}, {type:"folder", id, name, items:[キャラ要約...]} ] }` を返す。
  - `PUT /api/my-layout`（要ログイン）: IDだけのレイアウト `{ v: 1, items: [ {type:"character", id}, {type:"folder", id, name, items:["キャラID", ...]} ] }` を保存する。
  - 検証（400）: 形の不正、IDの形式、キャラIDの重複、フォルダの入れ子、フォルダ名（1〜30文字）、フォルダは100個まで。本文は64KBまで（413）。他人のキャラIDは、エラーにせず取り除く。
- ローカル確認: APIは `wrangler dev`（8787）で起動し、`.dev.vars` の `EXTRA_ALLOWED_ORIGINS` でだけ localhost を許可する。フロントは localhost で開いたときだけ、ローカルAPIに接続する。

### 機能要件
1. ログイン中の「あなたのキャラクター」一覧で、キャラの行をドラッグして上下に並べ替えられる。挿入位置は線で示す。
2. 並び順はサーバーに保存し、リロードしても、別の端末・ブラウザでも同じ順で表示される。
3. 並び順は、シノビガミとマギロギで共通の1つ。
4. 絞り込みタブを選んだ状態でも並べ替えられる。隠れている他ゲームのキャラの位置は変わらない。
5. 新しく保存したキャラは、一覧の一番上に入る。
6. キャラを削除しても、残りの順番は変わらない。
7. 行のクリックでキャラを開く動作と、削除ボタンは今までどおり動く。
8. 並び順の保存に失敗したら、元の順に戻して失敗を知らせる。

### 非機能要件
- 互換性: `GET /api/my-characters` と、既存のキャラデータ・保存処理は変更しない。デプロイの順は API → フロント。
- 性能: 一覧の取得は1回の通信。並べ替え1回につき保存の通信が1回。保存は順番に送る。
- 上限: 本文は64KBまで、フォルダは100個まで、フォルダ名は30文字まで。
- セキュリティ: ログインが必要。他人のキャラIDは保存時に取り除き、他人のキャラの情報は返さない。
- 障害時: レイアウトが壊れていても、一覧は新しい順で表示される。
- 費用: 今あるD1のテーブルを1つ増やすだけ。
- テスト: APIは vitest。フロントは手動で確認する。
- 同時編集: 後から保存した方が勝つ（競合は検知しない）。
- 保守: 両アプリの `index.js` に同じ処理を、既存の書き方に合わせて入れる。

### スコープ外
- フォルダのUI（B）、判定ツール（C）
- スマホ・タブレットでの並べ替え（タッチ端末では、行をドラッグできないようにする）
- ゲスト履歴の並べ替え、キーボードだけでの並べ替え
- 複数タブでの同時編集の競合検知
- 413/415の理由表示などの既存の課題、レート制限（保留中）

### タスク（上から順に実行。`←` の後は先に終わっている必要があるタスク。1タスク＝1コミット、T9・T10は運用作業）
- [x] T0a `sinobigami-api`: 前回の未コミット分をコミットし、`feature/my-layout` ブランチを作る
- [x] T0b `TRPG`: `CLAUDE.md` とこの計画書をコミットし、`feature/my-character-order` ブランチを作る
- [x] T1 API: ローカル開発用のCORS許可（`EXTRA_ALLOWED_ORIGINS`）← T0a
- [x] T2 API: マイグレーション `0005_character_layouts.sql` ← T0a
- [x] T3 API: バックアップの対象に `character_layouts` を追加する ← T2
- [x] T4 API: `GET` / `PUT /api/my-layout` とテスト ← T2
- [x] T5 フロント: localhost で開いたときは、ローカルAPIに接続する ← T1
- [x] T6 フロント: 一覧を保存された順で表示する（両アプリ）← T4, T5
- [x] T7 フロント: シノビガミでドラッグして並べ替え、保存する ← T6
- [x] T8 フロント: マギロギにも同じ処理を入れる ← T7
- [x] T9 APIの本番反映（バックアップ → マイグレーション → デプロイ → スモークテスト。実行前に確認を取る）← T2, T3, T4
- [x] T10 フロントを `main` にマージして push する（実行前に確認を取る）← T8, T9
- [x] T11a `sinobigami-api` の開発ログに記録する ← T9
- [x] T11b `TRPG/CLAUDE.md` のAPI契約とこの計画書を更新する ← T10

### 結果（2026-09-26 完了）
- API（`sinobigami-api` の `main`）: `c7a3cc3`, `5219a8f`, `d2ce384`, `e904a44`。本番のWorkerは Version `d3aa0cfc-aa19-4c53-abba-f270e3f23df0`。
  - バックアップは `20260926-034833`。
  - 自動モードの安全チェックでClaudeからの実行が拒否されたため、バックアップ・マイグレーション・デプロイはユーザーが実行した。
- フロント（`main`）: `5ba5a4a`, `a1753dc`, `1da4632`, `ee3f3f6`。
- ローカルで確認したこと:
  - 並べ替えと、画面・サーバーへの反映
  - 両アプリで同じ順になること
  - 絞り込み中は、他ゲームの位置が変わらないこと
  - 新規キャラが一番上に入ること、削除しても残りの順番が変わらないこと
  - クリックでキャラが開くこと
  - 保存に失敗したとき、元の順に戻って通知が出ること
  - タッチ端末では、行をドラッグできないこと
- マギロギのドラッグは、ブラウザ操作の座標が安定しなかったため、ドラッグのイベントを直接送って確認した（処理はシノビガミと同じ）。
- 挿入位置の線は、スタイルの値で確認した。実際の見た目と、ログイン中の本番での動作は、公開後にユーザーが確認する。
- 本番のスモークテスト: `/api/my-layout` の未認証は401、`/api/my-characters` は401。localhost は許可されていない。
- 詳細は `sinobigami-api/docs/development-log.md` の 2026-09-26 の節。
